// Collection.JournalEntry.after.update(function (userId, doc, fieldNames, modifier, options) {
//     Meteor.defer(function () {
//         let journal = Collection.JournalEntry.findOne(doc._id);
//         if(journal) {
//             return journal;
//         }
//     });
// });